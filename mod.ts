/**
 * @module
 * @bounded-systems/dev-contracts — the DevContracts framework, consolidated.
 *
 * The canonical Zod schemas for declarative project-config contracts live under
 * `./spec`; `contracts.toml` maps the JSON schemas the framework validates
 * against. (Migrated from the split @dev-contracts/@devcontracts packages +
 * personal-repo submodules into one @bounded-systems package.)
 */
export * from "./spec/mod.ts";
