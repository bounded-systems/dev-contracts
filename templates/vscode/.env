# VSCode template environment variables
export PUSHD_DEVTOOLS_VSCODE_TEMPLATE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PUSHD_DEVTOOLS_VSCODE_SETTINGS_PATH="${PUSHD_DEVTOOLS_VSCODE_TEMPLATE_DIR}/.vscode/settings.json"
