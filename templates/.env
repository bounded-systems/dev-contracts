# Set templates directory
export PUSHD_TEMPLATES_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Source template-specific environment files
for dir in */ ; do
  if [ -f "${dir}.env" ]; then
    source "${dir}.env"
  fi
done 
